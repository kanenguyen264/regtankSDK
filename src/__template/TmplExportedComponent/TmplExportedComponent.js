import React from "react";
import PropTypes from "prop-types";
import clsx from "clsx";
import { compose } from "recompose";

const TmplExportedComponent = compose()(function TmplExportedComponent(props) {
  return null;
});

export default TmplExportedComponent;
