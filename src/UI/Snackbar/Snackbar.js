import React from "react";
import PropTypes from "prop-types";
import clsx from "clsx";
import { compose } from "recompose";

const Snackbar = compose()(function Snackbar(props) {
  return null;
});

export default Snackbar;
